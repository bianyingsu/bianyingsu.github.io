# 协议
---
aliases:
  - 协议
标题: 协议
---
**CMPP协议参考**


**笔记本：** 协议


**创建时…** 2021/11/16 11:46 **更新时…** 2021/11/16 11:56


**作者：** 彼岸樱速


**CMPP协议参考**
一、CMPP协议简介
中国移动通信互联网短信网关接口协议(China Mobile Peer to
Peer CMPP)，是中国移动梦网内部各SMS参与节点相互交换SMS
的官方协议。作为梦网的参与方，移动梦网的增值服务商(Service
Provider SP )要按照此协议规范实现SP的部分，才可以将自己的短
信通过移动的GSM网络的数据通道传输到最终手机用户上。
实际上，协议规范了3个方面的内容：
。SP与移动的互联网短信网关（Internet Short Message
Gateway,ISMG）之间的接口协议
。ISMG之间的接口协议（譬如移动各省、市之间的短信息交换通
过ISMG之间进行）
。ISMG与汇接网关(Gateway Name Server GNS，类似互联网上
的DNS服务器)之间的接口协议，譬如跨省之类的短信需要GNS的
帮助指出当前ISMG该如何传递短信。
其中，后二方面属于移动短信息系统内部实现，对于SP来讲大概可
以“透明”来看待，只要实现了SP同ISMG的正确交互，就可以实
现接入移动梦网短信系统。我们关心的只是SP端的开发细节。

二、CMPP交互模式
从手机用户角度讲，按短信的发起/接收路径来讲，有两个叫法：

这两类短信交互，从SP端来看，都是属于Socket传输应用，CMPP
的协议是以TCP/IP协议作为底层承载协议的，属于TCP/IP协议栈之
上的应用。
SP同ISMG的交互连接分 **长连接** 和 **短连接** 。
所谓短连接，就是一次连接，传输一个消息，然后等待回复后拆除
连接，显然，效率很低，所以，基本上不被考虑.所谓长连接，就是
SP建立同ISMG连接，然后不断将数据包（一个个CMPP消息）发
送到ISMG，此处发送不必等待某条消息的ISMG回应消息返回，就
接着发送下一个消息。同时，等待ISMG返回信息或者等待ISMG发
送给SP的消息。发送同接收消息不是一定要同步的，实际采用异步
（同时也时双工）模式。从效率上，显然，必须全双工的异步模式
才能够满足实际应用需求。



<img src="/img/协议.pdf-0-0.png">
如下图（摘自CMPP2.0官方文档）所示，演示了长连接模式数据传
输过程：

三、SP端开发
1. 消息分类
首先，图中的CMPP消息有很多种，SP同ISMG之间交流这些消
息。大体上这些消息发出后，对方往往需要回复一个应答（RESP）
类消息。注意，这些消息大多具有方向性，也就是说只能够从一端
到另一端，而不可反方向进行，有些（少数）则可两端都能够发
出。以下信息主要来源于移动的文档，但针对大家易混淆或源文档
解释不够详细做了明确和补充。具体见下表：






|消息名|传递方向|解释说明|
|---|---|---|
|CMPP_CONNECT|SP---<br>àISMG|CMPP_CONNECT操作的<br>的是SP向ISMG注册作为一<br>个合法SP身份，此消息需<br>向ISMG发出验证信息，验<br>证方式采用md5加密密码<br>式，若注册成功后即建立<br>应用层的连接(否则ISMG会<br>立即断开Socket)，此后S<br>可以通过此ISMG接收和发<br>送短信。<br>ISMG以<br>CMPP_CONNECT_RESP<br>息响应SP的请求。具体的<br>法实现参考CMPP2.0文档<br>本文附件代码。|
|CMPP_CONNECT_RESP|SPß---<br>ISMG|ISMG对CMPP_CONNEC<br>消息的回复（无论是否验<br>成功）；如果未通过，会<br>消息中包含参考信息，但<br>ISMG会立即断开连接。|



CMPP­_ACTIVE_TEST SP ßà ISMG 这个消息通信双方都可以
出，目的是在没有其他消
发送时，保持双方的通信
路的连接，避免系统认为
信通道已经关闭。每一个
到此消息的实体应当返回
CMPP_ACTIVE_TEST_RE
消息，以“礼节性”表示
己的还在通信，维持数据
接有效性。
不过，据网友交流，有些
家实现的ISMG，仅仅靠自
己发出此消息等待SP回答
CMPP_ACTIVE_TEST_RE
来确定数据链路的有效性
而忽略SP的


|Col1|Col2|CMPP_ACTIVE_TEST消息 （有些霸道吧？）这个值 注意，不要仅仅实现发送 不响应此消息，避免数据 接失效。|
|---|---|---|
|CMPP_ACTIVE_TEST_RESP|SPßàISMG|对通信的另一端的<br>CMPP_ACTIVE_TEST消息<br>的回复。作用参考<br>CMPP_ACTIVE_TEST的解<br>释。|
|CMPP_SUBMIT|SP---<br>àISMG|在正确建立了数据连接后<br>SP向ISMG发送一个SMS数<br>据包。本消息需要仔细研<br>究。接收到此消息后，<br>ISMG需要以<br>CMPP_SUBMIT_RESP消息<br>作为回答。如果在一定时<br>时间内（移动给出的参考<br>60秒）内未得到消息回应<br>那么SP需要重新发送此数<br>包，以确保消息得到投递<br>如果重发达到3次后仍然得<br>不到回应，SP端应该考虑<br>能ISMG已经失效，应当停<br>止发送此短消息。|
|CMPP_SUBMIT_RESP|SPß---<br>ISMG|该消息由ISMG发送给SP，<br>同时返回一个“收条”(源<br>CMPP_SUBMIT消息的<br>ISMG端的标示MSGID)给<br>SP，表示“我ISMG已经确<br>认收到你这条消息了”。<br>到此消息后，SP需要保留<br>“收条”，因为后面ISMG<br>会最终报告本消息是否正<br>发送到用户手机。那个报<br>就是以此消息的“收条”<br>为确认那一条消息的。|
|CMPP_QUERY|SP---<br>àISMG|这个查询不是查询单条消<br>的，是查询SP发送给ISM<br>的短信的业务情况。可以<br>总计数，还可以分类查询<br>（基本就是发起对移动sm<br>业务数据库的查询统计）|
|CMPP_QUERY_RESP|SPß---<br>ISMG|ISMG将查询的数据返回给<br>SP。|


CMPP_CANCEL SP--

à ISMG



而忽略SP的


SP发起的取消某条消息的


令消息 其中包含了之前


|Col1|àISMG|令消息 其中包含了之前<br>，<br>经发送给ISMG消息的“收<br>条”以便ISMG可以确定是<br>那 条消息 如果消息已<br>一 。<br>发送给用户了 那么此消<br>，<br>息/命令会无效 ISMG返<br>，<br>失败<br>。|
|---|---|---|
|CMPP_CANCEL_RES|SPß---<br>ISMG|ISMG返回的对<br>CMPP_CANCEL的回复，<br>告知是否删除成功。|
|CMPP_DELIVER|SPß---<br>ISMG|当有MO或者状态报告时，<br>ISMG发送此消息。注意，<br>此消息的数据可以是用户<br>机发送给SP的消息，也可<br>对于之前SP发送到ISMG的<br>短信的最终状态的回复，<br>告短信的最终状态。|
|CMPP_DELIVER_RESP|SP---<br>àISMG|SP礼节性的回复告知收到<br>CMPP_DELIVER消息。要<br>出SP报告的<br>CMPP_DELIVER消息的<br>MSGID，以便ISMG知道那<br>一条消息SP已经确认收到|
|CMPP_TERMINAT|SPßàISMG|SP和ISMG都可以主动发消<br>息给对方，自己这端由于<br>种原因需要终止当前的数<br>连接。终止后，要经过重<br>Connection（验证）之后<br>才可以（进入事务阶段）<br>送SMS数据消息。|
|CMPP_TERMINATE_RES|SPßàISMG|通知对方，本端已经最好<br>除连接的准备。|


2. 交互阶段
整个CMPP协议交互分为 **验证** 、 **事务** 两个阶段。验证阶段，发送
CMPP_CONNECTION消息进行验证，通过验证后（必须要通过
才）进入CMPP事务阶段，可以发送短信数据了。上表中的
CMPP_CONNECTION以下的消息都属于事务阶段的消息。

3. 消息数据结构
每一个消息包含 **消息头** 和 **消息体** 两个部分，头固定长度为12字
节，其他消息长度各异，但是同一类型消息的长度是固定的。所有
消息的各个字段基本上仅有3种类型：Unsigned Integer （无符号
整型） 、Integer（整型）、Octet String（字符串），每种类型
具体长度不定，网络字节顺序。


1、 消息头(3个Unsigned Integer字段组成)：
4字节的Total_Length （Unsigned Integer），包含了此消息的总
计（包括了头部分）长度。
4字节的Command_Id（Unsigned Integer），指明了此消息到底
是什么消息，就是上表中消息的枚举值。应用程序根据此值确定本
数据包到底是什么消息，从而可以按照确定的消息类型，解析余下
的消息体。
4字节的Sequence_Id（Unsigned Integer），指明了此数据包在
发送此消息端的唯一编号。这个唯一编号，实际上可以看作流水操
作编号。因为分析到交互模式我们看到，SP发送数据到ISMG，不
是每发送一个就停下来等待ISMG的回复，而是“一下子”发送多
个数据包过去，然后等待ISMG的回应。然而，怎么知道回应的消
息是到底对应之前发送过去的消息中的那一条呢？本字段就是解决
此难题。SP按照编号发送消息过去，等待ISMG的回应—一般情形
下回应消息数据结构都有表明本消息回应的是SP发出的哪一条消
息，这个对应就是依靠Sequence_Id。它并不要求一定要严格唯
一，但是在给定的一段时间内，必须唯一（基本上只要SP发送过去
的消息中没有重复就行了）。如果是需要SP回答的消息，SP也必须
将ISMG发送过来的消息的Sequence_Id填入相应字段，表明这是
某个消息的回应。SP端和ISMG端Sequence_ID都没有确定具体的
算法。SP可以(但不推荐)采用数据库的唯一Id作为此值。

2、消息体。消息体长度根据消息不同，长度不一。其他的参考移
动的文档《中国移动通信互联网短信网关接口协议(China Mobile
Peer to Peer, CMPP)（V2.0）》，这里着重讲讲2个重要消息的消
息体数据结构：
CMPP­_SUBMIT的消息体：






|字段名|长度(byte)|类型|描述|
|---|---|---|---|
|Msg_Id|8|Unsigned<br>Integer|信息标识，应<br>侧ISMG本身<br>处填空，供I<br>时使用。SP提<br>应当留空。|
|Pk_total|1|Unsigned<br>Integer|相同Msg_Id<br>条数，从1开<br>一条消息长度<br>短信，可能需<br>多条消息，那<br>这多条消息属<br>整消息，所以<br>此给分解得到<br>信进行编号，<br>需要编成多少<br>此处就填写多|
|Pk_number|1|Unsigned<br>Integer|相同Msg_Id<br>号，从1开始<br>定消息的相对|


|Col1|Col2|g|定消息的相对|
|---|---|---|---|
|Registered_Delivery|1|Unsigned<br>Integer|是否要求返回<br>报告：<br>0：不需要<br>1：需要<br>2：产生SMC<br>类型短信仅供<br>使用，不发送<br>端）。<br>一般情况下，<br>认报告。SM<br>需要返回是否<br>告。这条消息<br>SMC时，当你<br>息给移动的I<br>动的计费系统<br>扣除用户的信<br>是此消息不会<br>手机。但是注<br>ISMG厂商(很<br>动要求)实现<br>候，如果你并<br>任何此包月类<br>给用户手机，<br>扣费行为的。<br>为这是属于违<br>收费”行为，<br>移动的合作关|
|Msg_level|1|Unsigned<br>Integer|信息级别，信<br>级。不过实际<br>觉ISMG端并<br>优先级。|
|Service_Id|10|Octet<br>String|业务类型，是<br>母和符号的组<br>表示业务的字<br>给发出的短信<br>过此字段大约<br>每个服务项目<br>量，有利于统<br>以及结算。|
|Fee_UserType|1|Unsigned<br>Integer|计费用户类型<br>0：对目的终<br>MSISDN计费<br>1：对源终端<br>计费；<br>2：对SP计费<br>3：表示本字<br>对谁计费参见<br>Fee_termin<br>段。|


|Fee terminal Id<br>_ _|21|Unsigned<br>Integer|被计费用户的<br>本字节填空<br>，<br>字段无效 对<br>，<br>见Fee User<br>本_字段与<br>段<br>，<br>Fee UserTy<br>0 1_ 2时互<br>、 、|
|---|---|---|---|
|TP_pId|1|Unsigned<br>Integer|GSM协议类<br>是解释请参考<br>GSM03.40中|
|TP_udhi|1|Unsigned<br>Integer|GSM协议类<br>是解释请参考<br>GSM03.40中<br>9.2.3.23,仅使<br>右对齐|
|Msg_Fmt|1|Unsigned<br>Integer|信息格式<br>0：ASCII串<br>3：短信写卡<br>4：二进制信<br>8：UCS2编<br>15：含GB汉<br>这个决定了<br>Msg_Conte<br>字节内容应该<br>编码来解码/|
|Msg_src|6|Octet<br>String|信息内容来源<br>业代码)，例<br>919000。|
|FeeType|2|Octet<br>String|资费类别<br>01：对“计费<br>码”免费<br>02：对“计费<br>码”按条计信<br>03：对“计费<br>码”按包月收<br>04：对“计费<br>码”的信息费<br>05：对“计费<br>码”的收费是<br>现。<br>通常值为02<br>是一个字符串<br>型。|
|FeeCode|6|Octet<br>String|资费代码（<br>位），如：<br>“0050”代|


|Col1|Col2|g|币050元<br>. 。|
|---|---|---|---|
|ValId_Time|17|Octet<br>String|存活有效期，<br>SMPP3.3协议|
|At_Time|17|Octet<br>String|定时发送时间<br>循SMPP3.3协<br>个字段可以让<br>定的时间给手<br>一般情况下不<br>为空字符串。|
|Src_Id|21|Octet<br>String|源号码<br>SP的服务代码<br>为服务代码的<br>网关将该号码<br>到SMPP协议<br>Submit_SM<br>的source_ad<br>该号码最终在<br>上显示为短消<br>号码。实际上<br>代码，可以是|
|DestUsr_tl|1|Unsigned<br>Integer|接收信息的用<br>(小于100个用<br>常是1。移动<br>条消息发给多<br>的。|
|Dest_terminal_Id|21*DestUsr_tl|Octet<br>String|接收短信的M<br>码，一个类似<br>组的结构。受<br>DestUsr_tl的<br>定了本字段的|
|Msg_Length|1|Unsigned<br>Integer|信息长度(M<br>为0时：<16<br>个字节)。如<br>码，可以达到<br>文字母。原因<br>文字母仅占用<br>中文等双字节<br>16位，同时每<br>节最高为都占<br>最多140个字<br>是70个汉字。|
|Msg_Content||||
|Msg_Content||||


