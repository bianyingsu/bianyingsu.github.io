# 类型转换
---
aliases:
  - 类型转换
标题: 类型转换
---
**byte[]和InputStream的相互转换**


**笔记本：** 类型转换


**创建时间：** 2023/12/11 18:53 **更新时间：** 2023/12/11 18:55



<img src="/img/类型转换.pdf-0-0.png">0-0

<img src="/img/类型转换.pdf-0-1.png">0-1

<img src="/img/类型转换.pdf-0-2.png">0-2
**对象与map互换,List`<`T`>`与List`<`Map`>`互换_list`<`map`>`**


**笔记本：** 类型转换


**创建时间：** 2023/12/11 17:46 **更新时间：** 2023/12/11 18:52

# **对象与map互换,List`<`T`>`与List`<`Map`>`互换**



<img src="/img/类型转换.pdf-1-0.png">1-0
```
* @param maps

* @return

```

```
* @param clazz

```

```
* @throws InstantiationException

* @throws IllegalAccessException

*/

public static `<`T`>` List`<`T`>` mapsToObjects(List<Map`<`String, Object`>` `>` maps,Class`<`T`>` clazz) t

List`<`T`>` list = Lists.newArrayList();IllegalAccessException {
if (maps != null && maps.size() `>` 0) {

Map`<`String, Object`>` map = null;

T bean = null;

for (int i = 0,size = maps.size(); i `<` size; i++) {

map = maps.get(i);

bean = clazz.newInstance();

mapToBean(map, bean);

list.add(bean);

}

}

return list;

}

}

```

